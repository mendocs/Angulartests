import { IputFieldDirective } from './iput-field.directive';

describe('IputFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new IputFieldDirective();
    expect(directive).toBeTruthy();
  });
});
