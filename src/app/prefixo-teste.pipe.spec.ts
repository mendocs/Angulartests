import { PrefixoTestePipe } from './prefixo-teste.pipe';

describe('PrefixoTestePipe', () => {
  it('create an instance', () => {
    const pipe = new PrefixoTestePipe();
    expect(pipe).toBeTruthy();
  });
});
